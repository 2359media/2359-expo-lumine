import React from 'react';
import { ScrollView } from 'react-native';
import { Props } from './shared';
export declare const SFScrollView: React.ForwardRefExoticComponent<Props & React.RefAttributes<ScrollView>>;
